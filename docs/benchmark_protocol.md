# Benchmark Protocol

Each benchmark case should specify:

- scene setup
- object count and clutter level
- target instruction
- allowed retries
- success criteria
- failure taxonomy
- seeds and repeat count
