# Apps

This directory contains user-facing entry points:

- `run_task.py`: run a single instruction through the stack
- `run_benchmark.py`: execute a benchmark suite
- `replay_trace.py`: replay a saved execution trace
